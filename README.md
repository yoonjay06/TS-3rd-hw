## 과제 3 - success/empty 관련

`{ status: 'success', data: [] }`도 타입 에러 없이 만들어진다. `empty`는 아예 데이터가 없는 상태고 `success`는 데이터가 있어야 하는 상태인데 그게 빈 배열이 된 것이라 의미는 다르지만, 화면에는 둘 다 게시글이 없는 것처럼 보인다. 그래서 게시글 0개라는 같은 상황을 두 상태로 표현할 수 있어서, 불가능한 상태를 완전히 없앤 건 아니다
